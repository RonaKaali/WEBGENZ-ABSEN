{
  "private": true,
  "scripts": {
    "build": "npm install --prefix admin-web && npm run build --prefix admin-web"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
